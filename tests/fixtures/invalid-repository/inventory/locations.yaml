locations:
  # VAL-GEN-002: invalid UUID
  - id: not-a-uuid-at-all
    code: room-a
    name: Room A

  # VAL-GEN-005: invalid code format (uppercase + spaces not allowed)
  - id: aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaab
    code: INVALID CODE
    name: Room B

  # VAL-GEN-006: duplicate id — same as room-a above
  - id: not-a-uuid-at-all
    code: room-a-dup
    name: Room A Duplicate
